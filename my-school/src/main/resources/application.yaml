server:
  port: 8081

spring:
  application:
    name: my-school

eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://my-service-discovery:8761/eureka/
    fetch-registry: true
    register-with-eureka: true